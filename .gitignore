node_modules
dist
.DS_Store
*.local
.env
server/data

# personal Claude skill bundles that may sit in this folder
*.skill

# stray reading material that lands in this folder — not project files
*.pdf

# local-voice sidecar venv (rebuilt by server/voice/setup.sh)
server/voice/env/
__pycache__/

# macOS/iCloud "name 2.ext" conflict duplicates — never repo files
**/* 2.js
**/* 2.jsx
# screen recordings dropped in for debugging — never repo content
*.mov

# native shell (Capacitor) — generated per sync/build
ios/App/App/public/
ios/App/build/
ios/App/App.xcodeproj/xcuserdata/
ios/App/App.xcworkspace/xcuserdata/
ios/DerivedData/
ios/App/CapApp-SPM/.build/

# the CC0 base mesh the anatomy model is built from (50 MB, fetched on demand)
tools/anatomy/.cache/
tools/anatomy/__pycache__/
